# 小鸟运动逻辑修复说明

## 问题分析
根据对 Cocos Creator 3.x 中 tween 用法的研究，发现之前实现中存在的问题：

1. 错误使用了 by() 方法：by() 方法是对当前值的相对变化，连续使用会导致位置累积偏移
2. 缓动序列结构不正确：tween(this.node) 和 sequence() 的配合使用不当

## 修复内容

### 1. 使用 to() 方法替代 by() 方法
- 不再使用相对变化的 by() 方法，而是使用绝对定位的 to() 方法
- 通过预先计算目标位置，确保小鸟移动到确切的目标坐标

### 2. 正确的缓动序列结构
- 分别创建上升和下降的 to() 动画
- 使用独立的 tween() 实例创建序列
- 使用 target() 方法指定目标节点
- 使用 start() 启动动画

### 3. 运动轨迹优化
- 向左飞：先移动到左上方位置，再移动到更靠左的原始高度位置
- 向右飞：先移动到右上方位置，再移动到更靠右的原始高度位置
- 使用 sineOut 和 sineIn 缓动函数实现平滑的抛物线轨迹

## 实现细节
- 上升阶段：使用 sineOut 缓动，模拟加速上升的效果
- 下降阶段：使用 sineIn 缓动，模拟减速下降的效果
- 每个动作都有明确的目标位置，避免了位置累积问题
- 保持了原有的动作中断和下落逻辑

## 优势
- 修复了运动轨迹不正常的问题
- 小鸟的运动路径更加准确和可预测
- 符合 Cocos Creator 3.x 的 tween 用法规范
- 保持了原有的功能特性