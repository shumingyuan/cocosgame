# 小鸟运动逻辑设计说明

## 需求分析
根据用户需求，实现了抖音平台竖屏小游戏中小鸟的运动逻辑：
1. 通过按屏幕两边完成所有动作
2. 小鸟有两个动作：向左飞和向右飞，分别通过触摸左侧屏幕和右侧屏幕触发
3. 单次运动的轨迹是反抛物线，两个动作可以相互打断实现向上运动
4. 在没有动作时小鸟沿着抛物线轨迹一直运动到地面

## 实现的功能

### 1. 触摸控制
- 使用 TOUCH_START 事件监听屏幕触摸
- 通过比较触摸点 X 坐标与屏幕中心点来判断左右
- 左侧触摸触发向左飞，右侧触摸触发向右飞

### 2. 反抛物线轨迹运动
- 向左飞：先向上向左移动（quintOut 缓动），然后向下向左移动（quartIn 缓动）
- 向右飞：先向上向右移动（quintOut 缓动），然后向下向右移动（quartIn 缓动）
- 使用 tween 动画实现平滑的抛物线轨迹

### 3. 动作中断机制
- 使用 isMoving 状态变量跟踪当前是否在执行移动
- 在执行新动作前先停止当前动作（tween(this.node).stop()）
- 保证两个动作可以相互打断

### 4. 自然下落
- 在 update 函数中检测小鸟是否在空中且非移动状态
- 如果满足条件，执行向下掉落的抛物线轨迹
- 使用 quartIn 缓动实现自然的下落效果

### 5. 飞行动画
- 循环播放飞行动画精灵帧
- 使用独立的动画计时器控制帧切换

### 6. 音效播放
- 在每次飞行动作时播放音效

## 关键技术点
- 使用 cc.tween 实现复杂的运动轨迹
- 使用 easing 函数实现抛物线效果（quintOut 上升，quartIn 下降）
- 通过触摸坐标判断左右操作区域
- 动作状态管理避免冲突
- 持续监控小鸟状态实现自动下落